.groups
  - current_user.groups.each do |group|
    .group{ :id => (group.id == @group.id ? "selected" : nil)}
      = link_to group_messages_path(group) do
        %p.group__group-name
          = group.name
        %p.group__latest-message
          = group.show_last_message